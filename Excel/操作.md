
### 去掉vlookup的错误返回值
如果是vlookup的返回值是#NA， 使用`Ifna` 函数将vlookup的错误返回值变成空

```shell
IFNA(#NA, "")
IFNA(VLOOKUP(B2, table_name!A:B, 2, False), "")
```

# Sheet操作

### 快速选中sheet
在左下角sheet的左右按钮，右键可以查看sheet列表
`ctrl + g` 输入 `sheetname!A1` 就可以找到指定的sheet